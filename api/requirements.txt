# Direct dependencies only, pinned to the versions from the image that built and
# smoke-tested successfully (docker run --rm ghostkube/api:dev pip freeze).
# Transitive deps are deliberately left to pip's resolver - a full freeze dump is
# unreadable and pins things that should be free to move.
#
# Kept in lock-step with the root requirements.txt. Do not let them drift: an
# index embedded by one version and queried by another is the failure mode to
# avoid, not the version numbers themselves.
fastapi==0.141.1
uvicorn==0.52.1
chromadb==1.5.9
sentence-transformers==5.6.1
crawl4ai==0.9.2
pydantic==2.13.4
python-dotenv==1.2.2
requests==2.34.2
kubernetes==34.1.0
groq==1.6.0
torch==2.13.0
transformers==5.14.1
scikit-learn==1.9.0
