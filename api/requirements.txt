# Direct dependencies only, pinned to the versions from the image that built and
# smoke-tested successfully (docker run --rm ghostkube/api:dev pip freeze).
# Transitive deps are deliberately left to pip's resolver - a full freeze dump is
# unreadable and pins things that should be free to move.
#
# NOTE: these are all NEWER than the root requirements.txt, which is pinned to
# the local venv (e.g. sentence-transformers 5.6.1 here vs 5.2.0 there). The two
# environments return different retrieval rankings for the same query against the
# same index - see the Phase 6 note in GhostKube_Guide.md. Align them before
# trusting a local search result to predict container behaviour.
fastapi==0.141.1
uvicorn==0.52.1
chromadb==1.5.9
sentence-transformers==5.6.1
crawl4ai==0.9.2
pydantic==2.13.4
python-dotenv==1.2.2
requests==2.34.2
